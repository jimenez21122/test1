using System;
using System.Collections.Generic;
using System.Text;

namespace test1
{
    class MathResolver
    {
        public static double SolveEquation(double a, double b, double c)
        {
            double aux, aux1, x;
            aux = b * b - 4.0 * a * c ;
            aux1 = System.Math.Sqrt(aux);
            x 
        }     
    }
}
